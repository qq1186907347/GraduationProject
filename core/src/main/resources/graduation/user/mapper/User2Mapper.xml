<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="graduation.user.mapper.User2Mapper">
    <resultMap id="BaseResultMap" type="graduation.user.dto.User2">
        <result column="USER_ID" property="userId" jdbcType="DECIMAL"/>
        <result column="USER_NAME" property="userName" jdbcType="VARCHAR"/>
        <result column="REAL_NAME" property="realName" jdbcType="VARCHAR"/>
        <result column="PASSWORD" property="password" jdbcType="VARCHAR"/>
        <result column="PHONE" property="phone" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectByUserName" resultMap="BaseResultMap" parameterType="graduation.user.dto.User2">
        SELECT * from
        login_user
        where
        user_name = #{userName,jdbcType=VARCHAR}
    </select>

    <select id="checkLogin" resultMap="BaseResultMap" parameterType="graduation.user.dto.User2">
        SELECT * from
        login_user
        where
        user_name = #{userName,jdbcType=VARCHAR}
        and password = #{password,jdbcType=VARCHAR}
    </select>

    <insert id="register">
    INSERT  INTO login_user (user_name, password, phone,real_name)
    VALUES (#{userName}, #{password}, #{phone},#{realName})
</insert>
</mapper>